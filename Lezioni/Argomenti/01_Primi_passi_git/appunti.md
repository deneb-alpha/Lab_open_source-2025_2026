Appunti comandi terminale dal 26/11/2025

- git commit -M= per creare un commit con un messagio.
- git branch= per vedere la branch.
- git branch -a= stampa la branch dove mi trovo(verde) e i remotes(rosso).
- git branch -vv= stampa le informazioni della branch.
- git fetch --all= va a sincronizzare le nuove informaioni create.
- git checkout -b= serve per passare da una branch ad un'altra.
- git push= inserisce la branch nelle etichette.
- git status= stampa lo stato della branch.
- git pull= aggiorna in locale quello che abbiamo in remoto.
- git diff= stampa le modifiche apportate al commit.
- git add nomefile.md= creo un file di testo.
- git log= stampa il commit,(deve essere già stato creato).

